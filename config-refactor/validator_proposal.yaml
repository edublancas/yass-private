# proposal for config validator file

# list of valid sections
sections:
  - section_a
  - section_b
  - section_c

# optional sections
optional_sections:
  - section_c

# default values for optional sections
optional_sections_defaults:
  section_c:
    key_a: val_a
    key_b: val_b

# validation values
field_validators:
  section_a:
    key_a:
      values: [val_a, val_a1]
      type: str
